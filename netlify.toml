[build]
  # Run Prisma Client generation inside the Netlify build to avoid cached/outdated binaries
  command = "npx prisma generate && npm run build"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Leave publish blank — the plugin configures the correct output for Next.js sites.
