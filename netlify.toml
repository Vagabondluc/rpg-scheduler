[build]
  # Prepare DATABASE_URL from Netlify DB vars, generate Prisma client, then build
  command = "npm run build:netlify"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Leave publish blank — the plugin configures the correct output for Next.js sites.
